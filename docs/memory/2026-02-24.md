# 2026-02-24 — First session

## Key events
- First boot. Met Lindau. Chose name **Astra**.
- Lindau started new role as **Observability Platform Engineer at Corti.ai**, Copenhagen.
- Set up Telegram integration:
  - Bot: @SLindauBot
  - Group chat ID: -5159390673
  - groupPolicy: allowlist, requireMention: false
  - Fixed trailing comma in openclaw.json that could cause parse errors
  - Doctor reports healthy: Telegram ok, 5 plugins loaded, 15 eligible skills
- **Action needed:** Lindau should rotate the bot token (it was exposed in chat) and disable BotFather privacy mode (/setprivacy → Disable) so the bot can see all group messages.
- Installed clawddocs skill from ClawHub (zip download, unpacked to workspace/skills/clawddocs)

## Lessons learned
- OpenClaw Telegram group config uses `groups: { "<chat_id>": { "requireMention": bool } }` — NOT `allowlistedChats`. Got this wrong initially and crashed the agent. Don't guess config formats; check the actual file first.
- Always read the real config before suggesting edits.
